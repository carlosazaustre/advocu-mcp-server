/**
 * @fileoverview Event format types for activities
 * @description Enum defining the available event formats for activities
 */

/**
 * Enum for event formats in activities
 * Used to specify whether an event is conducted in-person, virtually, or in a hybrid format
 */
export enum EventFormat {
  IN_PERSON = "In-Person",
  VIRTUAL = "Virtual",
  HYBRID = "Hybrid",
}
